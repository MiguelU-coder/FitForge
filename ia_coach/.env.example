# FitForge AI Coach — Environment Configuration
# Copy to .env and fill in your values

# App
APP_ENV=development
APP_PORT=8001

# JWT (same values as backend/.env)
JWT_ACCESS_SECRET=your-jwt-access-secret
SUPABASE_URL=https://your-project.supabase.co

# Redis (shared with ia_microservices)
REDIS_URL=redis://localhost:6379

# OpenRouter — Free LLM API (register at openrouter.ai)
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free

# ExerciseDB via RapidAPI
EXERCISE_DB_API_KEY=your-rapidapi-key
EXERCISE_DB_HOST=exercisedb.p.rapidapi.com
